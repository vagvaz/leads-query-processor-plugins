'''
Created on Jan 21, 2014

@author: nonlinear
'''

# addtocartbutton_regex = "^.*(add|put).?(in|to).?(bag|cart|basket|wishlist).*$"
addtocartbutton_regex = "^.*(add|put).?(in|to).?(bag|cart|basket).*$"

productname_divattr_regex = "^(id|class|(data-.+))$"

productname_divattrval = [["product"],["info","name","head","detail","title"]]